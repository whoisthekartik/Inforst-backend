config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 10  # 10 users per second
scenarios:
  - flow:
      - get:
          url: "/api/health"   # Replace with your actual route